/**
 * Controller For Home Page
 */
'use strict';
angular.module('hrss.si.enterprise.homeModule', [])
    .controller('HomeCtrl', ['$scope', '$rootScope',
        function ($scope, $rootScope) {
            //目前主要是站位
            // $scope.isShowView = false;
            // $rootScope.$on('loadUserDetail', function (evt, data) {
            //   $scope.isShowView = true;
            // })
        }]);
